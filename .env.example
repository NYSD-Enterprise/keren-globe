# Copy to ".env" in this folder before running docker compose:
#   cp .env.example .env && echo "JWT_SECRET=$(openssl rand -hex 32)" >> .env
# Shared by auth-service (signs login tokens) and api-gateway (verifies them).
JWT_SECRET=change-me-to-a-long-random-string
