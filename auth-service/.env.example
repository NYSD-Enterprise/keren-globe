# Copy this file to ".env" in this same folder before running docker compose.
# No secrets are currently required by auth-service, but the file must exist
# because docker-compose.yml loads it via "env_file". Add real values here
# (e.g. MONGO_URI, JWT_SECRET) once auth-service starts using them.
