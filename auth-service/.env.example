# Copy this file to ".env" in this same folder before running docker compose.
# MONGO_URI is already set in docker-compose.yml (points at the "mongo" container).
# JWT_SECRET is required to sign/verify login tokens - set it to a long random string.
JWT_SECRET=change-me-to-a-long-random-string
